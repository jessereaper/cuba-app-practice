source "https://rubygems.org" #this is where ur browser gets a gem file
ruby "2.5.0" # make sure this matches ur current version on ruby

#gem "rack" #this is the gem im using
gem 'cuba'

gem 'tilt'

gem "sqlite3"
